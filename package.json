{
  "name": "claude-pet",
  "version": "0.1.0",
  "description": "A desktop pet that grows as you code with Claude Code (state engine).",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test"
  }
}
